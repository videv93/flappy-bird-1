# Database
# Get your connection string from Railway PostgreSQL
DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"

# Better Auth
# Generate a secure secret (min 32 characters): openssl rand -base64 32
BETTER_AUTH_SECRET="your-secure-secret-min-32-chars"
BETTER_AUTH_URL="http://localhost:3000"

# Client-side Better Auth URL (required for browser auth client)
NEXT_PUBLIC_BETTER_AUTH_URL="http://localhost:3000"

# Google OAuth
# Create credentials at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Apple OAuth (Sign In with Apple)
# Configure at: https://developer.apple.com/account/resources/identifiers/list/serviceId
#
# Setup steps:
# 1. Create an App ID with "Sign In with Apple" capability
# 2. Create a Services ID (this is your APPLE_CLIENT_ID for web)
# 3. Configure domains and return URLs for your Services ID
# 4. Generate a private key with "Sign In with Apple" enabled
# 5. Generate the client secret JWT (see docs below)
#
# APPLE_CLIENT_ID: Your Services ID (e.g., com.yourapp.webapp)
# APPLE_CLIENT_SECRET: JWT token generated from your private key
#   - Generate using: https://developer.apple.com/documentation/sign_in_with_apple/generate_and_validate_tokens
#   - JWT expires every 6 months - must regenerate
#   - Use Team ID, Key ID, and private key (.p8 file) to generate
#
# Return URL to configure in Apple Developer Portal:
#   Development: http://localhost:3000/api/auth/callback/apple
#   Production: https://yourdomain.com/api/auth/callback/apple
APPLE_CLIENT_ID="your-apple-services-id"
APPLE_CLIENT_SECRET="your-apple-client-secret-jwt"

# Pusher (Real-time Notifications)
# Create a Pusher Channels app at: https://dashboard.pusher.com
PUSHER_APP_ID="your-pusher-app-id"
PUSHER_KEY="your-pusher-key"
PUSHER_SECRET="your-pusher-secret"
PUSHER_CLUSTER="your-pusher-cluster"
NEXT_PUBLIC_PUSHER_KEY="your-pusher-key"
NEXT_PUBLIC_PUSHER_CLUSTER="your-pusher-cluster"

# Polar (Payment Processing)
# Create an organization access token at: https://polar.sh/settings
# Create a one-time product ($9.99 Premium) and copy its Product ID
POLAR_ACCESS_TOKEN="polar_pat_your-access-token"
POLAR_WEBHOOK_SECRET="whsec_your-webhook-secret"
POLAR_PRODUCT_PREMIUM_ID="prod_your-premium-product-id"
# Set to "sandbox" for development, "production" for live payments
POLAR_SERVER="sandbox"

# Note: Copy this file to .env.local and replace with your actual values
# NEVER commit .env.local to git
